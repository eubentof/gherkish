<?php

beforeEach(function () {
    /** @Given different shared state exists */
    $this->state = true;
});

test('uses shared state', function () {
    /** @When the scenario runs */
    $result = $this->state;

    /** @Then it succeeds */
    expect($result)->toBeTrue();
});
